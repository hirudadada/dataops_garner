# APP_ENV: development
APP_ENV=${DEV_APP_ENV}
# # Default
# APP_ENV: production

# optional, will use db_host with db_name if opt out
SERVICE_NAME=${DEV_APM_SERVICE}

SLEEP_INTERVAL=${DEV_SLEEP_INTERVAL}
