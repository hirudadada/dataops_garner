apiVersion: v1
kind: ConfigMap
metadata:
  name: <%= app %>-service-configmap
data:
  APP_ENV: <%= ENV.fetch('APP_ENV', 'development') %>
  # APP_ENV: test
  # APP_ENV: production

  SLEEP_INTERVAL: '<%=ENV['SLEEP_INTERVAL'] %>'
