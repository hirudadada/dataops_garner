apiVersion: v1
kind: ConfigMap
metadata:
  name: <%= app %>-credentials-configmap
data:
  # APM
  ELASTIC_APM_SECRET_TOKEN_ENCRYPTED: <%= ENV['ELASTIC_APM_SECRET_TOKEN_ENCRYPTED'] %>

  # Database
  DB_USER: <%= ENV['DB_USER'] %>
  DB_PASSWORD_ENCRYPTED: <%= db_password_encrypted %>
